<Page x:Class="finance_tracker_comp586.views.Login"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:finance_tracker_comp586.views"
      xmlns:clearableTextBox="clr-namespace:finance_tracker_comp586.views.user_controls"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Login">

    <Page.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#9B111E" Offset="0"/>
            <GradientStop Color="#964B00" Offset="1"/>
        </LinearGradientBrush>
    </Page.Background>

    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
            <RowDefinition Height="3*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="5*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Text="Login" Foreground="White" FontSize="32" Margin="5"/>

        <StackPanel Grid.Row="1" Grid.Column="1" Orientation="Vertical">
            <StackPanel Orientation="Horizontal" Margin="5">
                <TextBlock Text="Username" Width="75" FontSize="16" Foreground="White" VerticalAlignment="Center"/>
                <clearableTextBox:ClearableTextBox x:Name="UsernameTextBox" Placeholder="Enter username" Height="30" Width="250" IsPassword="False"/>
            </StackPanel>
            <StackPanel Orientation="Horizontal" Margin="5">
                <TextBlock Text="Password" Width="75" FontSize="16" Foreground="White" VerticalAlignment="Center"/>
                <clearableTextBox:ClearableTextBox x:Name="PasswordTextBox" Placeholder="Enter password" Height="30" Width="250" IsPassword="True"/>
            </StackPanel>
            <Grid>
                <Button Content="Login" Width="50" Height="25" Margin="5" BorderBrush="Transparent" Background="White" HorizontalContentAlignment="Center" VerticalContentAlignment="Center" HorizontalAlignment="Center" VerticalAlignment="Center" Click="Login_Button_Click"/>
            </Grid>
        </StackPanel>

    </Grid>
</Page>
