<Page x:Class="finance_tracker_comp586.views.LoginView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:clearableTextBox="clr-namespace:finance_tracker_comp586.views.user_controls"
      mc:Ignorable="d"
      d:DesignHeight="750"
      d:DesignWidth="1000"
      Title="Login">

    <Page.Resources>
        <SolidColorBrush x:Key="PrimaryOrange" Color="Orange"/>
        <SolidColorBrush x:Key="PrimaryWhite" Color="White"/>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Background" Value="{StaticResource PrimaryOrange}"/>
            <Setter Property="Foreground" Value="{StaticResource PrimaryWhite}"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
        </Style>

        <LinearGradientBrush x:Key="LoginBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="#9B111E" Offset="0"/>
            <GradientStop Color="#964B00" Offset="1"/>
        </LinearGradientBrush>
    </Page.Resources>

    <Grid Background="{StaticResource LoginBackground}">
        <StackPanel Width="300"
                    VerticalAlignment="Center"
                    HorizontalAlignment="Center">

            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Welcome"
                           FontSize="32"
                           Foreground="White"
                           Margin="5"
                           HorizontalAlignment="Center"/>

                <TextBlock Text="Login to an existing account."
                           FontSize="16"
                           Foreground="White"
                           Margin="5"
                           HorizontalAlignment="Center"
                           TextAlignment="Center"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <clearableTextBox:ClearableTextBox
                x:Name="UsernameTextBox"
                Placeholder="Username"
                Height="30"
                Margin="5"
                IsPassword="False"/>

            <clearableTextBox:ClearableTextBox
                x:Name="PasswordTextBox"
                Placeholder="Password"
                Height="30"
                Margin="5"
                IsPassword="True"/>

            <Button Content="Login"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="Login_Button_Click"/>

            <Button Content="Register for an Account"
                    Style="{StaticResource PrimaryButtonStyle}"
                    Click="Register_Button_Click"/>

        </StackPanel>
    </Grid>
</Page>