<Page x:Class="finance_tracker_comp586.views.HomeView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:local="clr-namespace:finance_tracker_comp586.views"
      mc:Ignorable="d" 
      d:DesignHeight="750" d:DesignWidth="1000"
      Title="Home">
    <Page.Resources>
        
        <SolidColorBrush x:Key="PrimaryRed" Color="DarkRed"/>
        <SolidColorBrush x:Key="PrimaryWhite" Color="White"/>
        <SolidColorBrush x:Key="PrimaryBlack" Color="Black"/>
        <LinearGradientBrush x:Key="HomeBackground" StartPoint="0,0" EndPoint="1,1">
            <GradientStop Color="DarkRed" Offset="0"/>
            <GradientStop Color="DarkOrange" Offset="1"/>
        </LinearGradientBrush>

        <Style x:Key="CardButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="250"/>
            <Setter Property="Height" Value="150"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="White"
                        CornerRadius="15"
                        Padding="15"
                                Margin="10">
                            <ContentPresenter ContentSource="Content" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="Top"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Page.Resources>
    
    <Page.Background>
        <StaticResource ResourceKey="HomeBackground"/>
    </Page.Background>
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <TextBlock Grid.Row="0" Text="Home" Foreground="{StaticResource PrimaryWhite}" FontSize="32" Margin="5" HorizontalAlignment="Left"/>
        <TextBlock Grid.Row="0" Grid.Column="2" Text="{Binding Name}" Foreground="{StaticResource PrimaryWhite}" FontSize="16" HorizontalAlignment="Center" VerticalAlignment="Center"/>
        <StackPanel Grid.Row="2" Grid.Column="1" Orientation="Vertical" VerticalAlignment="Center" HorizontalAlignment="Center">
            <TextBlock Text="Dashboard" Foreground="{StaticResource PrimaryWhite}" FontSize="32" HorizontalAlignment="Center" VerticalAlignment="Center"/>
            <StackPanel Orientation="Horizontal">
                
                <Button Style="{StaticResource CardButtonStyle}" HorizontalContentAlignment="Left" Click="Wallet_Button_Click">
                    <StackPanel>
                        <TextBlock Text="Wallet" Foreground="Black" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Text="Balance:" Foreground="Black" FontSize="16"/>
                        <TextBlock Text="Monthly Budget:" Foreground="Black" FontSize="16"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource CardButtonStyle}" HorizontalContentAlignment="Left" Click="Savings_Button_Click">
                    <StackPanel>
                        <TextBlock Text="Savings" Foreground="Black" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Text="Total Savings:" Foreground="Black" FontSize="16"/>
                        <TextBlock Text="Current APY:" Foreground="Black" FontSize="16"/>
                    </StackPanel>
                </Button>

                <Button Style="{StaticResource CardButtonStyle}" HorizontalContentAlignment="Left" Click="Brokerage_Button_Click">
                    <StackPanel>
                        <TextBlock Text="Brokerage" Foreground="Black" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>
                        <TextBlock Text="*show stock with change*" Foreground="Black" FontSize="16"/>
                    </StackPanel>
                </Button>

            </StackPanel>
        </StackPanel>
    </Grid>
    
</Page>